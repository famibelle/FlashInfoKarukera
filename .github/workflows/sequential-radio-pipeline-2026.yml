name: 🚀 Orchestrateur Radio Séquentiel

on:
  workflow_dispatch:
    inputs:
      verbose:
        description: "Verbose"
        type: boolean
        required: false
        default: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "workflow_dispatch OK"
